/*
 * musl_stubs/fork_impl.h — Empty stub.
 *
 * musl's malloc.c defines __malloc_atfork() itself.
 * This file only needs the include guard to satisfy the #include.
 */

#ifndef MUSL_STUB_FORK_IMPL_H
#define MUSL_STUB_FORK_IMPL_H

/* __malloc_atfork is defined in musl's malloc.c — no stub needed */

#endif
